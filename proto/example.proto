syntax = "proto3";

package server;

option go_package = "./proto";

service Examples {
  rpc DoExample (Request) returns (Response) {}
}

message Request {
    string id = 1;
    string name = 2;
}

message Response {
  bool status = 1;
  string data = 2;
  string message = 3;
}